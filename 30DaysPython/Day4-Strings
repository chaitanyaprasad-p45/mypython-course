#   1.  Concatenate the string 'Thirty', 'Days', 'Of', 'Python' to a single string, 'Thirty Days Of Python'.
a = "Thirty"
b = "Days"
c = "Of"
d = "Python"
space = " "
print(a + space + b + space + c + space + d)

#   2.  Concatenate the string 'Coding', 'For' , 'All' to a single string, 'Coding For All'.

#   3.  Declare a variable named company and assign it to an initial value "Coding For All".
company = "Coding For All"

#   4.  Print the variable company using print().
print(company)

#   5.  Print the length of the company string using len() method and print().
print(len(company))

#   6.  Change all the characters to uppercase letters using upper() method.
print(company.upper())

#   7. Change all the characters to lowercase letters using lower() method.
print(company.lower()) 

#   8.  Use capitalize(), title(), swapcase() methods to format the value of the string Coding For All.
print(company.capitalize()) #   Coding for all
print(company.title())      #   Coding For All
print(company.swapcase())   #   cODING fOR aLL

#   9.  Cut(slice) out the first word of Coding For All string.
first_space_index = company.find(" ")  # Find the index of the first space
sliced_text = company[first_space_index + 1:]  # Slice from the character after the space
print(sliced_text)

#   10. Check if Coding For All string contains a word Coding using the method index, find or other methods.
print(company.find('Coding'))    # Returns the index if found, -1 if not found
print(company.index("Coding"))  # Returns the index if found, raises ValueError if not found
print("Coding" in company)  # Returns True if found, False if not

#   11. Replace the word coding in the string 'Coding For All' to Python.   
new_company = company.replace("Coding", "Python")
print(new_company)